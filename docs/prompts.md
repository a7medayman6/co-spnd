The design should be mobile-first, but should also work well and be visually appealing on desktop screens. Use responsive design principles to ensure a seamless experience across all devices. and use frontend-design plugin skills.

Add a logo to the application, ideally in the header, that represents the Co-Spnd brand. The logo should be simple, memorable, and work well in both small and large sizes. Consider using a symbol or icon that reflects the concept of shared spending or financial management.

Password field in signup and signin forms should have a toggle to show/hide the password for better usability.

On vercel deployment of the web application, when refreshing on a non-root route, it shows a 404 error. fix it.

I will migrate the app from 2 repositories to one monorepo. The frontend will be in a folder called `co-spnd-web` and the backend in `co-spnd-api`. Update all paths and references accordingly. and remove the github repos inside each, and commit the changes incrementally to the new repo in the root folder. make sure to update vercel config for each repo.

Implement a landing page that clearly communicates the value proposition of Co-Spnd. The landing page should include a brief description of the app, its key features, and a call-to-action to sign up or learn more. Use engaging visuals and concise copy to capture the user's attention.
Should be at least 5 sections long.
Include star us on github as well using this link: https://github.com/a7medayman6/co-spnd